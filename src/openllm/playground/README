This folder represents a playground and source of truth for a features
development around OpenLLM

I include this in here so that you can run this from the Pip package easily.
Refer to each script docstring for more information.

python -m openllm.playground.general --help

python -m openllm.plaground.ft_opt_lora --help

NOte that this folder will be considered 'breaking' most of the cases, so use will care.
